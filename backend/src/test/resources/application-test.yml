spring:
  datasource:
    url: jdbc:postgresql://localhost:5432/tms
    username: tms
    password: tms
    driver-class-name: org.postgresql.Driver
  flyway:
    enabled: false
  jpa:
    open-in-view: false
    hibernate:
      ddl-auto: validate
    properties:
      hibernate:
        default_schema: tms
