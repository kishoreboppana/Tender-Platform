-- Demo seed data source moved to classpath:db/seed/demo-data.json
-- (loaded on startup when tender.seed.enabled=true).
-- Flyway V2 seed remains for databases created before file-based loading.
-- Edit demo-data.json to change demo tenders, customers, and dashboard defaults.

SELECT 1;
